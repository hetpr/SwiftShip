# SwiftShip
wiftShip is a modular web service application built using the Flask framework in Python, demonstrating the principles of Service-Oriented Architecture (SOA). The system consists of multiple independent microservices — a Frontend Service, Route Service, and ETA Service.
